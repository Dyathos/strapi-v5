'use strict';

/**
 * formulaire-professeur service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::formulaire-professeur.formulaire-professeur');
