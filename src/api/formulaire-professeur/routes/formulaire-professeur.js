'use strict';

/**
 * formulaire-professeur router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::formulaire-professeur.formulaire-professeur');
