'use strict';

/**
 * formulaire-simple service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::formulaire-simple.formulaire-simple');
