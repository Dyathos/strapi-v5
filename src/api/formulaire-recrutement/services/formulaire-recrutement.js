'use strict';

/**
 * formulaire-recrutement service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::formulaire-recrutement.formulaire-recrutement');
