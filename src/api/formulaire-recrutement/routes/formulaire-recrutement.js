'use strict';

/**
 * formulaire-recrutement router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::formulaire-recrutement.formulaire-recrutement');
