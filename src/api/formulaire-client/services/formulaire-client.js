'use strict';

/**
 * formulaire-client service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::formulaire-client.formulaire-client');
